Чтобы запустить сервер, достаточно запустить его командой
```python3 server.py```

Используются бибилотеки FastAPI, pydantic и peewee, поэтому может потребоваться их установка:

```
pip install fastapi
pip install uvicorn[standart]
pip install pydantic 
pip install peewee
```